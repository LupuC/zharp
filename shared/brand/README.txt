Zharp brand package

Open guide.html for the full design guide. It reads the same tokens the
site uses, so if you change tokens/tokens.css the guide changes with it.

logo/     master vectors, lockups, wordmark, diagrams
icons/    every raster size, favicon.ico, apple touch, maskable
social/   1200x630 share card
tokens/   tokens.css and tokens.json
misuse/   the failure cases illustrated in the guide

Mark geometry, on a 46 by 44 body: bars 10, diagonal 21, dashes 7 tall at
16, 14 and 18 long, positioned x 32, 38, 32 and y 29, 43, 57. Clear space
is one bar on every side. Minimum size with dashes is 32 px, below that use
the reduced form without dashes.